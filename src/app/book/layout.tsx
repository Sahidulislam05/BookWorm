export const metadata = {
  title: "Book Details | BookWorm",
  description: "View book details and reviews",
};

export default function BookLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
